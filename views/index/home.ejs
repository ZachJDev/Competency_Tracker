<%-include('../partials/header')%>
 <div  class="body-content"> 
     <% if(isLoggedIn) { %> 
        <h1>Welcome, <%=name %>!</h1>
        <div class="dashboard">
            <div class="competencies">
                <h2>Most Recently Updated competencies:</h2>
                <ul class="dashboard-competences">
                    <% competencies.forEach(comp =>  { %>
                        <li class="competency">
                            <form action="/competencies/<%= comp._id %>" method="GET" > 
                             <button class="not-button">
                                 <h3 class="s-heading"><%=comp.number%>: <%=comp.name%></h3>
                             </button>
                             </form>
                   <% }) %> 
                </ul>
            </div>
            <div class="dashboard-roles">
                <h2>Most Recently Updated roles:</h2>
                <% roles.forEach(role =>  { %>
                    <li class="role">
                        <form action="/roles/<%= role._id %>" method="GET" > 
                         <button class="not-button">
                             <h3 class="s-heading"><%=role.name%></h3>
                         </button>
                         </form>
               <% }) %> 
            </div>
        </div>
     <% } else { %> 
        <h1>Welcome!</h1>
        <div class="invitation">
        <h3>Please <a>Log in</a> or <a>Sign up</a> to see your dashboard.</h3>
    </div>
     <% } %> 
<%-include('../partials/footer')%>