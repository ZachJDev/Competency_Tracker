<nav id="navbar">
    <div id="logo-container" class="container">
      <i class="fa fa-graduation-cap fa-5x"></i>
    <h1 class="logo-heading s-heading"><a class="logo-box-link" href="/"><% if(institution != null) { %> <%= institution %> <% } else { %> Institution <% } %></a></h1>
    <% if(!isLoggedIn) { %> 
      <div class="login">
        <a class="logo-box-link" href="/login">Login</a> <span>|</span> <a class="logo-box-link" href="/signup">Sign-up</a>
      </div>
    <% } else { %>
      <div class="logged-in-links">
        <form action="/logout" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button class="not-button" type="submit">
            <p class="logo-box-link">Logout</p>
          </button>
      </form>
      <span>|</span>
      <form action="/user" method="get">
        <button class="not-button" type="submit">
          <p class="logo-box-link"><%= name%></p>
        </button>
    </form>
      </div> 
    <% } %> 
      <i class="navmenu fa fa-bars" display="none"></i>
    </div>
    <div id="navigation" class="container nav-hidden">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/roles">Roles</a></li>
        <li><a href="/competencies">Competencies</a></li>
        <li class="mobile-login"><a href="#">Login</a> <span>|</span> <a href="#">Sign-up</a></li>
      </ul>
    </div>
  </nav>